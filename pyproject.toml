[project]
name = "laocaimi-content-engine"
version = "2.1.0"
description = "Rule-aware, evidence-gated, deduplicated lottery content knowledge and generation engine for laocaimi.org"
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
