name: production-controller-200-preflight-v2-temp

on:
  push:
    branches:
      - production/controller-200-20260813-v2
    paths:
      - .github/production-controller-200-preflight-v2.trigger

permissions:
  contents: read

jobs:
  preflight:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - name: Run zero-provider 200-article capacity preflight
        run: |
          set -euo pipefail
          python scripts/produce_articles_total.py 200 --output-dir runs/production-controller-200-preflight-v2 | tee production-controller-200-preflight-v2.log
      - uses: actions/upload-artifact@v4
        with:
          name: production-controller-200-preflight-v2
          path: |
            runs/production-controller-200-preflight-v2/
            production-controller-200-preflight-v2.log
          if-no-files-found: error
